@html.extend('layout', function(model) {

    @html.block('content', function(model) {
        
        <div class="row about">
            <div class="medium-6 large-8 columns">
                <h4>@model.meta.title</h4>

                @html.raw(model.content)
            </div>
        </div>
    })

})
